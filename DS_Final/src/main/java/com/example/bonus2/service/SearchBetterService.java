package com.example.bonus2.service;

import java.util.HashMap;

import org.springframework.stereotype.Service;

@Service
public class SearchBetterService {
    public HashMap<String, String> betterSearchResults(HashMap<String, String> searchResults) { 
        // 在這裡添加你的搜尋優化算法 
        // 例如：篩選掉無效結果、重新排序等
        //sublinks 
        // keyword counting
        //sorting
        return searchResults;
    }
}
